export const appSettings = Object.freeze({
  defaultLocale: 'en',
  supportedLocales: ['en', 'ru', 'zh-CN'],
  chineseText: {
    defaultPinyinVisible: true,
    defaultToneMarksVisible: true,
    defaultSimplified: true,
  },
})
