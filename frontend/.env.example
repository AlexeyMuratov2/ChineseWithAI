# API origin for fetch(). Leave empty in local dev: Vite proxies /api to Spring (vite.config.ts).
# Set for production builds when the UI is hosted on a different origin than the API, e.g.:
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
